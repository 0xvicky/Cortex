services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"

  worker:
    build: .
    command: python run_worker.py
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"